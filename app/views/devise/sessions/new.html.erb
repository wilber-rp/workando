<div class="container d-flex justify-content-center">
  <div class="custom-container">
    <h1>
      Encontre seu <span>match</span> perfeito no mercado de trabalho com o
      <span>workando</span>.
    </h1>

    <div class="card" id="container" data-controller="form-motion">
      <div
        class="form-container sign-up-container"
        data-controller="formvalidation"
      >
        <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "simple-form" }) do |f| %>
          <h3 id="sign-up-h3">Cadastre-se gratuitamente</h3>
          <div class="input-box">
            <%=
              f.input :email,
                      placeholder: 'E-mail',
                      label: false,
                      required: true,
                      autofocus: true,
                      input_html: {
                        autocomplete: 'email',
                        class: 'input',
                      }
            %>
          </div>
          <div class="input-box">
            <%=
              f.input :password,
                      required: true,
                      placeholder: 'Senha',
                      label: false,
                      hint:
                        (
                          if @minimum_password_length
                            "#{@minimum_password_length} characters minimum"
                          end
                        ),
                      input_html: {
                        autocomplete: 'new-password',
                        class: 'input',
                      }
            %>
          </div>
          <div class="input-box">
            <%=
              f.input :password_confirmation,
                      placeholder: 'Confirmar senha',
                      label: false,
                      required: true,
                      input_html: {
                        autocomplete: 'new-password',
                        class: 'input',
                      }
            %>
          </div>

          <div class="input-box">
            <%=
              f.input :first_name,
                      placeholder: 'Nome',
                      label: false,
                      required: false,
                      input_html: {
                        autocomplete: 'first_name',
                        class: 'input',
                      }
            %>
          </div>

          <div class="input-box">
            <%=
              f.input :last_name,
                      placeholder: 'Sobrenome',
                      label: false,
                      required: false,
                      input_html: {
                        autocomplete: 'last_name',
                        class: 'input',
                      }
            %>
          </div>

          <div class="input-box">
            <%=
              f.input :cpf,
                      placeholder: 'CPF',
                      label: false,
                      required: false,
                      input_html: {
                        autocomplete: 'cpf',
                        class: 'input',
                      }
            %>
          </div>

          <div class="input-box">
            <%=
              f.input :company_name,
                      placeholder: 'Razão Social',
                      label: false,
                      required: false,
                      input_html: {
                        autocomplete: 'company_name',
                        class: 'input',
                      }
            %>
          </div>

          <div class="input-box">
            <%=
              f.input :cnpj,
                      placeholder: 'CNPJ',
                      label: false,
                      required: false,
                      input_html: {
                        autocomplete: 'cnpj',
                        class: 'input',
                      }
            %>
          </div>

          <div class="input-box">
            <%=
              f.input :phone,
                      placeholder: 'Telefone com DDD',
                      label: false,
                      required: false,
                      input_html: {
                        autocomplete: 'phone',
                        class: 'input',
                      }
            %>
          </div>

          <div class="input-box">
            <%=
              f.input :zip,
                      placeholder: 'CEP',
                      label: false,
                      required: false,
                      input_html: {
                        autocomplete: 'zip',
                        class: 'input',
                      }
            %>
          </div>

          <div class="input-box">
            <%=
              f.input :address,
                      placeholder: 'Endereço',
                      label: false,
                      required: false,
                      input_html: {
                        autocomplete: 'address',
                        class: 'input',
                      }
            %>
          </div>

          <div class="input-box">
            <%=
              f.input :number,
                      placeholder: 'Número',
                      label: false,
                      required: false,
                      input_html: {
                        autocomplete: 'number',
                        class: 'input',
                      }
            %>
          </div>

          <div class="input-box">
            <%=
              f.input :complement,
                      placeholder: 'Complemento',
                      label: false,
                      required: false,
                      input_html: {
                        autocomplete: 'complement',
                        class: 'input',
                      }
            %>
          </div>

          <div class="input-box">
            <%=
              f.input :neighborhood,
                      placeholder: 'Bairro',
                      label: false,
                      required: false,
                      input_html: {
                        autocomplete: 'neighborhood',
                        class: 'input',
                      }
            %>
          </div>

          <div class="input-box">
            <%=
              f.input :city,
                      placeholder: 'Cidade',
                      label: false,
                      required: false,
                      input_html: {
                        autocomplete: 'city',
                        class: 'input',
                      }
            %>
          </div>

          <div class="input-box">
            <%=
              f.input :state,
                      placeholder: 'Estado',
                      label: false,
                      required: false,
                      input_html: {
                        autocomplete: 'state',
                        class: 'input',
                      }
            %>
          </div>

          <div class="input-box">
            <%=
              f.input :lat,
                      placeholder: 'Latitude',
                      label: false,
                      required: false,
                      input_html: {
                        autocomplete: 'lat',
                        class: 'input',
                      }
            %>
          </div>

          <div class="input-box">
            <%=
              f.input :long,
                      placeholder: 'Longitude',
                      label: false,
                      required: false,
                      input_html: {
                        autocomplete: 'long',
                        class: 'input',
                      }
            %>
          </div>



          <%= f.button :submit, 'INICIAR CADASTRO', class: 'btn' %>
        <% end %>
      </div>
      <div class="form-container sign-in-container">
        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "simple-form" }) do |f| %>
          <h3 id="sign-in-h3">Entrar</h3>
          <div class="input-box">
            <%=
              f.input :email,
                      label: false,
                      placeholder: 'e-mail',
                      required: true,
                      autofocus: true,
                      input_html: {
                        autocomplete: 'email',
                        class: 'input',
                      }
            %>
          </div>
          <div class="input-box">
            <%=
              f.input :password,
                      label: false,
                      placeholder: 'Senha',
                      required: true,
                      input_html: {
                        autocomplete: 'current-password',
                        class: 'input',
                      }
            %>
          </div>
          <div class="remember-box">
            <%=
              if devise_mapping.rememberable?
                f.input :remember_me, label: 'Mantenha-me logado', as: :boolean
              end
            %>
            <%=
              link_to 'Esqueceu a senha?',
                      new_password_path(resource_name),
                      class: 'link-pass-forg'
            %>
          </div>
          <%= f.button :submit, 'ENTRAR', class: 'btn' %>
        <% end %>
      </div>

      <div class="overlay-container">
        <div class="overlay">
          <div class="overlay-panel overlay-left">
            <h1 class="overlay-title text-center">Bom te ver por aqui!</h1>
            <p>Entre na sua conta e não deixe nenhuma oportunidade passar.</p>
            <button class="ghost btn" id="signIn">Entrar</button>
          </div>
          <div class="overlay-panel overlay-right">
            <h1 class="overlay-title">Olá!</h1>
            <p>Cadastre-se gratuitamente e não perca nenhum match.</p>
            <button class="ghost btn" id="signUp">Cadastrar</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
